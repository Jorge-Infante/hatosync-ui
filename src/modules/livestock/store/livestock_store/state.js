export default {
  animals: [], // animals of the ACTIVE farm (GET /livestock/animals/)
  // External genetics (GET /livestock/animals/?external=true): semen straws /
  // rental bulls, referenced as parents in genealogy but never part of the herd.
  externals: [],
}
