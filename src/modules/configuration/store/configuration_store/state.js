export default {
  breeds: [], // per-farm breed catalog (GET /configuration/breeds/)
  identificationTypes: [], // per-farm ID types (GET /configuration/identification-types/)
  medications: [], // per-farm medication catalog (GET /configuration/medications/)
  lots: [], // per-farm lot/management-group catalog (GET /configuration/lots/)
  inactivationReasons: [], // per-farm herd-exit reasons (GET /configuration/inactivation-reasons/)
}
